A dedicated snippet:

contract ForceSend {
    constructor() payable {}
    function boom(address target) external {
        selfdestruct(payable(target));
    }
}

Explains when forced ETH breaks assumptions in challenges:

balance checks

require(msg.value == 0)

invariant violations
